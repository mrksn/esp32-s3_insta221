# OpenOCD configuration for ESP32-S3 with built-in USB JTAG
# Use the ESP USB JTAG interface
source [find interface/esp_usb_jtag.cfg]

# Use the ESP32-S3 target configuration
source [find target/esp32s3.cfg]

# Optional: Set adapter speed (can be increased for faster flashing)
adapter speed 20000